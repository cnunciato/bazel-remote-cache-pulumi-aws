name: bazel-remote-cache-pulumi-aws
description: Deploys a Bazel remote cache powered by Amazon S3 and CloudFront.
runtime:
  name: nodejs
  options:
    packagemanager: npm
template:
  description: Deploys a Bazel remote cache powered by Amazon S3 and CloudFront.
  config:
    username:
      description: The HTTP basic-auth username (optional)
      secret: true
    password:
      description: The HTTP basic-auth password (optional)
      secret: true
