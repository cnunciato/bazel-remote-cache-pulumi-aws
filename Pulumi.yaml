name: ${PROJECT}
description: ${DESCRIPTION}
runtime: nodejs
template:
  description: A Bazel remote cache powered by Amazon S3 and CloudFront.
  config:
    username:
      description: The HTTP basic-auth username (optional)
      secret: true
    password:
      description: The HTTP basic-auth password (optional)
      secret: true
